import React from 'react';
import { SubmissionWaiting } from '@/components/layout/SubmissionWaiting';

/** 录入等待态页（模拟提交后流程，独立布局） */
export function SubmissionStatusPage() {
  return <SubmissionWaiting />;
}
