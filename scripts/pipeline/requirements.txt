# 数据管道依赖
# 沙箱实测环境：Python 3.11.1 / akshare 1.18.22 / pandas 3.0.0 / requests 2.32.5
#
# akshare 版本务必锁定：东财接口的列名在小版本间会漂移，
# 本管道的列名解析基于 1.18.22 实测结果编写（见 README「实测源矩阵」）。
akshare==1.18.22

# akshare 的隐式依赖，显式声明以便 CI 可复现
pandas>=2.0,<4.0
requests>=2.31,<3.0
